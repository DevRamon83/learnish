import {
  userType,
  currentPrivacy,
  currentTos,
  langsArray,
  plan,
} from "./atomicConstants.js";

const constants = {
  currentPrivacy,
  currentTos,
  userType,
  langsArray,
  plan,
};

export default constants;
